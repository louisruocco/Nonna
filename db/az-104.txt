 When you use the Bulk create users operation in the portal, you must specify four things | the display name, the UPN, the initial password, whether the account is enabled or disabled
 Dynamic groups aren't able to have users or devices added to it manually
 You should use the ID of the role in case the role name was changed to prevent such a change from breaking the script.
 3 ways to create Administrative Units | Portal, CLI, 365 Admin Center
 Azure Import/Export | Export Jobs only allow data to be exported from Blob Storage
 Only zoneredundant replication (ZRS) supports StorageV2, FileStorage, and BlockBlobStorage accounts and only standard storage accounts can be used
 File shares can be configured to use Azure AD Kerberos
 Before configuring object replication, you must enable blob versioning for both storage accounts, and you must enable the change feed for the source account.
 Live migration is only supported by accounts that have LRS or GRS replication
 Only General purpose v2 accounts support converting to ZRS
 Azure file sync can replicate files to different shares
 A sync group cannot have more than one cloud endpoint attached to it
 Immutability cannot be disabled after it has been enabled on the storage account
 VMs in a scale set should be in the same region as the scale set itself
 View ARM templates in RGs
 Maximum number of fault domains = 3
 Maximum number of update domains = 20
 App Service Plans are created as per the Operating System type
 You can detach a disk from a running virtual machine
 VMs can have more than one NIC | To add the VM to two subnets, the VM would need 2 NICs. One connected to one subnet and the other NIC to the other Subnet
 If you want to run a Docker container as an Azure web service, you must configure the Publish option and select Docker container.
 3 resources that can have delete locks on them: RGs, Subscriptions, Virtual Machines
 VMs cannot be load balanced if they are not in an availability set
 Azure DNS provides automatic registration of VMs from a single virtual network that's linked to a private zone as a registration virtual network.
 If a VM is not in the registration vnet or private DNS zone, it cannot resolve/register DNS requests
 Internal load balancers ONLY affect resources in the same vnet
 Azure DNS uses port 53
 Azure Site to Site VPNs with HA need a minimum of 2 IP addresses
 Steps to create a site to site VPN | 1. Create Gateway Subnet  2. Create a VPN Gateway 3. Create local network gateway 4. Create a VPN Connection
 VNet gateways need to be established to connect 2 VNets across different Subscriptions
 By default, all VMs in a Vnet can communicate with each other, regardless of what subnet they are in
 Azure resources connected to a VNet have outbound connectivity to the internet by default
 Azure NetworkWatcher is Regional
 3 main monitoring components of Azure Monitor Insight | Traffic, Connectivity, Network Health and Metrics, The component used to view NSG config status from Azure monitor insight is Traffic
 The number of action groups required is determined by the number of users needed to be notified
 Performance monitor helps to view network performance between various points in Azure network infrastructure
 By default, backups of virtual machines are kept for 30 days.
 Configure the virtual machine scale set from the availability options
 AzCopy supports ONLY Blob and File Storage
 Azure Monitor can send Emails every minute and can only send 1 text message every 5 minutes
